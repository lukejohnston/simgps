simgps: A simple GPS simulator

simgps generates NMEA sentences that simulate the movement of a GPS around a
path. It can read kml files such as those generated by Google Earth. It has a
graphical interface that uses tkInter.

To run simgps you'll need:

Python 2.6
pyserial v2.5 for python2.6
tkInter for python
